/**
 * Copy values from your Supabase project (anon key is safe for browser + RLS).
 * When the UI is served from the API at /ui, API requests use the same origin.
 */
window.AXB_CONFIG = {
  apiBase: "",
  supabaseUrl: "https://mbzkfjpvb.supabase.co",
  supabaseAnonKey: "YOUR_SUPABASE_ANON_KEY",
};
